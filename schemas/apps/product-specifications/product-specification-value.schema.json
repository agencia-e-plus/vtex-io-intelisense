{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "block title",
	"description": "Renders the product specification value. It should be declared as a children of `product-specification`. It is possible to render with `HTML`.",
	"type": "object",
	"properties": {
		"children": {
			"type": "array",
			"items": { "$ref": "../../shared/block.schema.json" }
		}
	}
}
