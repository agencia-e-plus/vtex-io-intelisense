{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "block title",
	"description": "",
	"type": "object",
	"properties": {
		"title": {
			"type": "string"
		},
		"props": {
			"type": "object",
			"description": "Block's properties",
			"properties": {
				"placeholderText": {
					"type": "string",
					"description": " Placeholder text for phone input.",
					"default": "Enter your email address (translated according to the store's locale)."
				},
				"inputLabel": {
					"type": "string",
					"description": "Label for the phone input.",
					"default": "null"
				},
				"errorMessage": {
					"type": "string",
					"description": "Error message to be shown if email is invalid.",
					"default": "Invalid phone"
				}
			}
		}
	}
}
