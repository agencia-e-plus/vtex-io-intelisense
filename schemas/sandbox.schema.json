{
	"type": "object",
	"title": "Sandbox Component",
	"description": "Allows mounting arbitrary HTML content in extension points from the comfort and safety of an iframe.",
	"properties": {
		"props": {
			"type": "object",
			"properties": {
				"initialContent": { "type": "string" },
				"width": { "type": "string" },
				"height": { "type": "string" },
				"allowCookies": { "type": "boolean", "default": false },
				"allowStyles": { "type": "boolean", "default": true },
				"hidden": { "type": "boolean", "default": false }
			}
		}
	}
}
